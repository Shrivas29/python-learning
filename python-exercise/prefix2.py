def common_prefix(str):
    str = ["flow","flower","float"]
    res = ""

    for i in range(len(str[0])):
        zero = "fl"
        if zero == range(len(str[1])):
            print(zero)
            if zero == range(len(str[2])):
                print(zero)
            else:
                print(res)
    return i
print(common_prefix(str))
        